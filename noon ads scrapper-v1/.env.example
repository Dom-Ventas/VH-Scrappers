# Copy this file to .env and fill in the values.

# API that returns the list of search terms to scrape.
# Your fetchQueries() adapter in src/api/queries.ts will read this.
# Left blank for now — the scrapper falls back to two sample queries when empty.
QUERIES_API_URL=

# API that receives scraped results. Your postScrapedResult() adapter
# in src/api/results.ts will POST to this URL.
# Left blank for now — posting throws until this is set.
RESULTS_API_URL=

# Bearer token (or whatever auth scheme your API uses).
API_TOKEN=

# Where the persistent Chrome profile + user-settings.json live.
# Leave empty to use the default: %LOCALAPPDATA%\NoonSearchTermScrapper\chrome-profile
SCRAPER_PROFILE_DIR=

# Delay between consecutive scrapes, in milliseconds.
# Default 30000 (30 seconds) to avoid noon rate-limiting.
SCRAPE_DELAY_MS=30000

# Default noon host + storefront for the first-run onboarding page.
# Noon serves every country from one host and scopes the storefront in the first
# path segment, so both parts are needed to build a URL.
DEFAULT_NOON_DOMAIN=www.noon.com
DEFAULT_NOON_LOCALE=uae-en

# Optional comma-separated marketplace short codes to scrape (empty = all).
# Example: SCRAPE_SHORT_CODES=NNAE
SCRAPE_SHORT_CODES=

# Show the Chrome scraper window on screen. Set to 1 to debug; otherwise the
# window launches off-screen so you can keep working in other apps while the
# scraper runs. The first run (when no user-settings.json exists) is always
# visible regardless of this setting, since the user has to interact with
# the setup form and sign into noon.
BROWSER_VISIBLE=
